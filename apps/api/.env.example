# CORS origin - the URL of the web app that can access this API
# For local dev: http://localhost:5173 (Vite default port)
# For production: your deployed web app URL
API_CORS_ORIGIN=http://localhost:5173

# Better Auth configuration
# Secret: used to sign session tokens - generate with: openssl rand -base64 32
# URL: the public URL of this API (where auth endpoints are accessible)
API_BETTER_AUTH_SECRET=
API_BETTER_AUTH_URL=http://localhost:8787
